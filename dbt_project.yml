name: 'paid_ads_project'
version: '1.0.0'
config-version: 2

profile: 'paid_ads_project'

source-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target"]

models:
  paid_ads_project:
    +schema: Paid_ads_test
    +materialized: view
